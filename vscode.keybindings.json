// Place your key bindings in this file to override the defaults
[
    { "key": "ctrl+tab", "command": "workbench.action.terminal.focus"},
    { "key": "ctrl+tab", "command": "workbench.action.focusActiveEditorGroup", "when": "terminalFocus"}
]